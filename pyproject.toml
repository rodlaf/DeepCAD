[project]
name = "DeepCAD"
version = "0.1.0"
description = "CAD Generation"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["cadlib"]